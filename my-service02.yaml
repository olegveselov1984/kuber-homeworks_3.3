apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: app
spec:
  ports:
  - name: backend-svc
    protocol: TCP
    port: 8082
    targetPort: 8082
  selector:
    app: backend
  type: ClusterIP # Можно не писать, по умолчанию